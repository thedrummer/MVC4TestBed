@model MVC4TestBed.Models.TemplateTest

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<fieldset>
    <legend>TemplateTest</legend>

    <div class="display-label">DateTimeField</div>
    <div class="display-field">@String.Format("{0:g}", Model.DateTimeField)</div>

    <div class="display-label">DateField</div>
    <div class="display-field">@String.Format("{0:g}", Model.DateField)</div>

    <div class="display-label">EmailAddress</div>
    <div class="display-field">@Model.EmailAddress</div>

    <div class="display-label">Password</div>
    <div class="display-field">@Model.Password</div>

    <div class="display-label">Text</div>
    <div class="display-field">@Model.Text</div>

    <div class="display-label">NormalString</div>
    <div class="display-field">@Model.NormalString</div>

    <div class="display-label">Url</div>
    <div class="display-field">@Model.Url</div>

    <div class="display-label">TelNo</div>
    <div class="display-field">@Model.TelNo</div>

    <div class="display-label">LongText</div>
    <div class="display-field">@Model.LongText</div>

    <div class="display-label">Month</div>
    <div class="display-field">@Model.Month</div>

    <div class="display-label">Week</div>
    <div class="display-field">@Model.Week</div>

    <div class="display-label">Currency</div>
    <div class="display-field">@Model.Currency</div>
</fieldset>
<p>
    @Html.ActionLink("Edit", "Edit", new { id=Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>


